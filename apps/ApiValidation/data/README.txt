Place optional validation assets here:

- test.jpg (or test.png): image load test
- font.ttf (optional): custom font for text test

If missing, the app falls back to a generated checker image and system fonts.
